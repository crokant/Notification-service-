package com.icispp.notificationservice.controllers;

import com.icispp.notificationservice.services.ManagerService;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;

public class ManagerController {
    /*
    private final ManagerService managerService;

    public ManagerController(ManagerService managerService) {
        this.managerService = managerService;
    }

    @PostMapping("/create-registration-link")
    public ResponseEntity<String> createRegistrationLink() {
        String link = managerService.createRegistrationLink();
        return ResponseEntity.ok(link);
    }

     */
}
